# ===================================
#  Form Field Definitions
# ===================================

fields:
    count:
        label: Must complete X amount of times
        type: number
    activity:
        label: Activity
        type: relation
        options:
            nameColumn: title
