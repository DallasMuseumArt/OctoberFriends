# ===================================
#  List Column Definitions
# ===================================

columns:
    title:
        label: Title
    uuid:
        label: UUID
    is_authorized:
        label: Authorized for barcode scanner
        type: switch
    description:
        label: Description
