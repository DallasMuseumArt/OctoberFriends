# ===================================
#  Form Field Definitions
# ===================================

fields:
    title:
        label: Title
        required: true;
    description:
        label: Description
        type: textarea
    mac_address:
        label: Mac Address
        description: Identify a device by mac address
    printer_reward:
        label: Print rewards to:
    printer_membership:
        label: Print membership cards to:
